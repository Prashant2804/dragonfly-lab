{
  "name": "dragonflylab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "dragonflylab.in \u2014 Aerom RTK GNSS marketing site",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node scripts/serve.mjs",
    "check": "node build.mjs && node scripts/check.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}